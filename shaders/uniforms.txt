#include "importantconstants.h"

#define MAX_LIGHTS 1


layout(set=0,binding=UNIFORM_SLOT,std140,row_major) uniform UBO{
    mat4 projMatrix;
    mat4 viewMatrix;
    mat4 viewProjMatrix;
    vec3 attenuation;
    vec3 eyePosition;
	mat4 light_viewMatrix;

	mat4 light_projMatrix;
	mat4 light_viewProjMatrix;
	vec3 light_eyePosition;
	vec3 light_hitherYon; //hither, yon, yon-hither
	vec3 light_look;
	vec3 hitherYon;
	vec3 look;

    vec4 lightPositionAndDirectionalFlag[MAX_LIGHTS];
    vec4 lightColorAndIntensity[MAX_LIGHTS];
    vec4 cosSpotAngles[MAX_LIGHTS];
    vec4 spotDirection[MAX_LIGHTS];
	
	mat4 flattenMatrix;

};
